# from the command line, type in the following commands:
# source ./setup
# and that's it. This only has to be done once.

git config pull.rebase false
git remote add startercode https://github.com/psu-ds410/template-Fall2024
